# Pilat Book

`Pilat-Book.woff2` is the webfont version of the existing Tempo brand font at
`apps/og/public/fonts/Pilat-Book.otf`. It preserves the source font's characters,
OpenType layout features, and naming metadata.
