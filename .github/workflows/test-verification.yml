name: Test Contract Verification

on:
  workflow_dispatch:
    inputs:
      networks:
        description: "Networks to test (comma-separated: devnet,mainnet,testnet)"
        required: false
        default: "devnet,mainnet,testnet"
      verifier-url:
        description: "Verifier URL"
        required: false
        default: "https://contracts.porto.workers.dev"
  schedule:
    - cron: "0 */6 * * *"

env:
  VERIFIER_URL: ${{ github.event.inputs.verifier-url || 'https://contracts.porto.workers.dev' }}

jobs:
  tempo-check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        network: [devnet, mainnet, testnet]
        include:
          - network: devnet
            rpc_url: https://rpc.devnet.tempoxyz.dev
          - network: mainnet
            rpc_url: https://rpc.mainnet.tempo.xyz
          - network: testnet
            rpc_url: https://rpc.testnet.tempo.xyz
    name: tempo-check (${{ matrix.network }})
    steps:
      - name: Check if network is selected
        id: check
        run: |
          NETWORKS="${{ github.event.inputs.networks || 'devnet,mainnet,testnet' }}"
          if [[ "$NETWORKS" == *"${{ matrix.network }}"* ]]; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
          else
            echo "skip=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Foundry
        if: steps.check.outputs.skip != 'true'
        uses: foundry-rs/foundry-toolchain@master
        with:
          version: nightly
          network: tempo

      - name: Fetch tempo-check.sh
        if: steps.check.outputs.skip != 'true'
        run: |
          curl -fsSL https://raw.githubusercontent.com/tempoxyz/tempo-foundry/tempo/.github/scripts/tempo-check.sh -o tempo-check.sh
          chmod +x tempo-check.sh

      - name: Run tempo-check
        if: steps.check.outputs.skip != 'true'
        env:
          ETH_RPC_URL: ${{ matrix.rpc_url }}
        run: ./tempo-check.sh